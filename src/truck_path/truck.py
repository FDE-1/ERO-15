
def find_truck_path(district):
    # problème du postier chinois
    # solution 1: chercher un chemin eulerien
    # solution 2: parcour profondeur

    """
    def parcours_camion(G):
    if nb_odd(G) %2 != 0:
        #parcours 1 edge d'un node impair
        parcours_1_vertrice(node_odd)
        remove(1_vertrice,G)
    trouver le chemin eulerian
    return chemin(G)